@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        // force a reload so auth state is re-evaluated
        Navigation.NavigateTo("/login", forceLoad: false);
    }
}
